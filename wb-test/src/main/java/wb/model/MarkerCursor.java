package wb.model;

public class MarkerCursor implements Cursor {

	@Override
	public void update(Board board) {
		// pick sprite based on angle
		// render shadow at cursorPosition offsets based on height
		// render marker at cursorPosition offsets based on height
	}

}
